# API

Fastify shell for the nutrition tracker modular monolith. It currently owns only
cross-cutting HTTP behavior; domain functionality belongs in modules registered
from `src/modules/v1.routes.ts` as it is implemented.

## Runtime configuration

| Variable | Default | Validation |
| --- | --- | --- |
| `API_HOST` | `127.0.0.1` | Non-empty string; containers set `0.0.0.0` explicitly |
| `API_PORT` | `3001` | Integer from 1 through 65535 |
| `LOG_LEVEL` | `info` | Pino log level |
| `NODE_ENV` | `development` | `development`, `test`, or `production` |
| `READINESS_TIMEOUT_MS` | `2000` | 10 through 60000 milliseconds |
| `SHUTDOWN_GRACE_MS` | `10000` | 100 through 300000 milliseconds |

`GET /health` is a process liveness check. `GET /ready` invokes the injected
dependency readiness probe. Domain routes are mounted below `/v1`.

Readiness checks receive an `AbortSignal`, are deadline-bounded, and run
single-flight. Dependency adapters must also configure driver/query timeouts and
stop promptly when the signal aborts.

Request IDs are generated by the API rather than trusted from caller headers.
HTTP lifecycle logs contain route templates and operational metadata only; they
exclude raw URLs, headers, query strings, and payloads. Error responses use an
RFC 9457-style `application/problem+json` envelope and never expose stack traces.
